" Use jkl; to navigate
noremap l k
noremap k j
noremap j h
noremap ; l

" Show cursor line's number
set number

" Use line numbers relative to the cursor line
set relativenumber

" Use the clipboard as the default register (https://vim.fandom.com/wiki/Accessing_the_system_clipboard)
set clipboard=unnamedplus
